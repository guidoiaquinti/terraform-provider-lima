---
page_title: "lima_disk Data Source - terraform-provider-lima"
description: |-
  Reads an existing Lima disk.
---
<!--
  GENERATED FILE - DO NOT EDIT.

  Source: templates/data-sources/disk.md.tmpl
  Regenerate: make docs      (make docs-check fails if this file disagrees)

  The attribute reference comes from the provider schema, so it cannot drift
  from the code. The narrative around it is hand-written in the template.
-->

# lima_disk (Data Source)

Reads an existing Lima disk. This data source never modifies the disk.

## Example usage

```hcl
data "lima_disk" "data" {
  name = "project-data"
}

output "guest_path" {
  value = data.lima_disk.data.mount_point
}

output "free_to_destroy" {
  value = data.lima_disk.data.in_use_by == null
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) The disk name, exactly as `limactl disk list` reports it.

### Optional

- `timeouts` (Attributes) (see [below for nested schema](#nestedatt--timeouts))

### Read-Only

- `dir` (String) The disk's directory inside `LIMA_HOME`.
- `format` (String) The format Lima reports for the stored image. Note that this is what was stored, not necessarily what was requested: the `vz` driver requires raw images.
- `in_use_by` (String) Name of the **running** instance holding this disk, or null. A disk attached to a stopped instance reports null.
- `mount_point` (String) Guest path the disk is mounted at when attached.
- `size` (String) Disk size in IEC notation, for example `50GiB`.
- `size_bytes` (Number) Disk size in bytes, as Lima reports it.

<a id="nestedatt--timeouts"></a>
### Nested Schema for `timeouts`

Optional:

- `read` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours).
